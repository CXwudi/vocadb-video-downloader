package common_role.task_producer;

/**
 * Just an interface telling this is a task producer, we really can not give a concrete declaration
 * if we want high extendability
 * @author CX无敌
 */
public interface GeneralTaskProducer {
}
